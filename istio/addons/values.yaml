kialiGateway:
  service:
    port: 20001

  istio:
    ingress:
      selector: ingressgateway
      annotations:
        external-dns.alpha.kubernetes.io/ttl: "5m"
    domain: istio-kiali.lomeone.com
