apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: istio-mesh-dashboard
spec:
  instanceSelector:
    matchLabels:
      dashboard: grafana
  configMapRef:
    name: istio-mesh-dashboard
    key: istio-mesh-dashboard.json
