apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: istio-control-plane-dashboard
spec:
  instanceSelector:
    matchLabels:
      dashboard: grafana
  configMapRef:
    name: istio-control-plane-dashboard
    key: istio-control-plane-dashboard.json
